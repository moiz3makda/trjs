// // for arrays
// push adds the element in the end of Array
// pop removes the last element of the array and returns it (so we can store it in the different variable)
// unshift adds the element to the starting of the Array
// shift removes the element from the starting of the array and it also returns that element like pop so we can store it in the diferent variable or we can directly use it 